define(['ember', 'templates'], function(Ember, templates) {
    var IndexController;
    return IndexController = Ember.Controller.extend({
        name: 'Handlebars (controller)',
        css: 'less (controller)'
    });
});